from typing import Optional, Annotated
from datetime import datetime
from pydantic import BaseModel, AfterValidator
from enum import Enum
from utils.util_functions import validate_name
{{#each entity.properties}}
    {{#if this.options}}
from models.{{../entity.name}} import {{this.type}}
    {{/if}}
{{/each}}

class {{entity.name}}View(BaseModel):
{{#each entity.properties}}
    {{snake this.name}}: {{#unless this.required}}Optional[{{/unless}}{{this.type}}{{#unless this.required}}]{{/unless}}
{{/each}}
