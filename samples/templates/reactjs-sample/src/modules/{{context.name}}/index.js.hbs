import React from 'react';
{{#each context.models}}
import AddEdit{{this.name}} from './pages.AddEdit{{this.name}}';
import List{{this.name}} from './pages.List{{this.name}}';
{{/each}}

export default {
    routeProps: [
        {
            key: 'List{{entity.name}}',
            path: '/{{kebab entity.name}}',
            component: List{{entity.name}},
        },
        {
            key: 'AddEdit{{entity.name}}',
            path: '/edit-{{kebab entity.name}}',
            component: AddEdit{{entity.name}},
        }
    ],
    name: '{{context.name}}',
};
